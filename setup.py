from setuptools import setup

setup(name='gpt',
      version='0.0.1',
      author='Li Yichao',
      packages=['gpt'],
      description='A PyTorch implementation of GPT',
      license='MIT',
      install_requires=[
            'torch',
      ],
)
